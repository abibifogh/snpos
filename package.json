{
  "name": "snpos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Restaurant POS on Appwrite — QR ordering, kitchen display, shift control, accounting",
  "scripts": {
    "provision": "node scripts/provision.mjs",
    "seed:admin": "node scripts/seed-admin.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-appwrite": "^14.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
